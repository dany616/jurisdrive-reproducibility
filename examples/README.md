# Synthetic smoke example

This directory contains one synthetic Korean traffic-collision record. It is
not copied or derived from the licensed AI Hub corpus. `verify_release.py` uses
it to exercise the portable manifest resolver and the graph, contract, and
dry-run stages from a clean clone.
